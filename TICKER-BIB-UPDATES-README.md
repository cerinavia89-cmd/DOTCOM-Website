# Latest Site Edits (Ticker + BIB layout)

## 72 Hour Deal Page (deal.html + 72-hour.html)
- Added favicon icons to every ticker pill (assets/images/favicon-16.png).
- Set ticker to full-width behavior via `.ticker--full` so it spans the same width as the hero image container.

## Business in a Box Page (business-in-a-box.html)
- Ticker now spans full width of the hero container (`.ticker--full` + grid span rule).
- Added favicon icons to every ticker pill (replaced any previous pill icons for consistency).
- Moved “What’s included” and “Who this is for” cards up by removing the extra top margin.
- Added “How it works” heading above the 1–2–3 step cards.

## CSS
Updated `assets/css/styles.css`:
- Added `.ticker--full` (and grid-span rule for `.grid-2`)
- Added `.pill-icon` sizing (16x16)
